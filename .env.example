# ── SergioLab Portfolio — variables de entorno ────────────────────────────────
# Copia este archivo como .env.local y rellena los valores.
# NUNCA subas .env.local a git (ya está en .gitignore).

# URL del Cloudflare Worker que hace de proxy para la API de Claude.
# Cuando lo tengas configurado, descomenta y rellena:
# VITE_CHAT_ENDPOINT=https://sergiolab-chat.tuuser.workers.dev/chat

# Instrucciones de configuración: ver docs/09_CLAUDE_CHATBOT_PROXY.md
